<!DOCTYPE html>
<html>
  <head>
    <meta charset="utf-8">
    <meta name="viewport" content="width=device-width,initial-scale=1">
    <title>Mail to Me !</title>
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>
    <%= stylesheet_link_tag 'application', media: 'all', 'data-turbolinks-track': 'reload' %>
    <%= javascript_include_tag 'application', 'data-turbolinks-track': 'reload' %>
  </head>

  <body>
    <div class="container-fluid">
      <% if user_signed_in? %>
        <% if action_name == 'personal' || (controller_name == 'travels' && action_name != 'show') || (controller_name == 'registrations') %>
          <%= render 'layouts/users_header' %>
          <%= render 'layouts/users_sidebar' %>
          <%= yield %>
          <%= render 'layouts/users_footer' %>
        <% else %>
          <%= render 'layouts/travels_header' %>
          <%= render 'layouts/travels_sidebar' %>
          <%= yield %>
          <%= render 'layouts/travels_footer' %>
        <% end %>
      <% elsif controller_name == 'users' %>
        <%= yield %>
      <% else %>
        <header class="navbar fixed-top bg-dark" id="header">logo</header>
        <%= yield %>
        <footer class="navbar fixed-bottom bg-dark" id="footer">url</footer>
      <% end %>
    </div>
  </body>
</html>
